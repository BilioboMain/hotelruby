        <!-- Page content-->
        <form action="/reviews/update", method="POST">
        <div class="container">
            <div class="row">
                <!-- Blog entries-->
                <div class="col-lg-10">
                    <h1>Все отзывы   <%= hidden_field_tag :authenticity_token, form_authenticity_token%>
  <button type="submit" class="btn btn-primary">Отправить</button> </h1>
                    <!-- Featured blog post-->
                    <!-- Nested row for non-featured blog posts-->
                    <div class="row">
                        <div class="col-lg-8">
                            <% @reviews.each do |review|%>
                                <div class="card mb-4">
                                <div class="card-body">
                                    <div class="small text-muted"><%=review.created_at%></div>
                                    <h2 class="card-title h4"><%= review.author_name%></h2>
                                    <p class="card-text"><%=review.body%></p>
                                     <input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
  <label class="form-check-label" for="flexCheckDefault">
    Default checkbox
  </label>
                                </div>  
                            </div>
                            <%end%>
                        </div>
                    </div>