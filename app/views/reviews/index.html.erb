        <!-- Page content-->
        <div class="container">
            <div class="row">
                <!-- Blog entries-->
                <div class="col-lg-10">
                    <h1>Все отзывы  <a class="btn btn-primary" href="/reviews/new" role="button">Оставить отзыв</a> </h1>
                    <!-- Featured blog post-->
                    <!-- Nested row for non-featured blog posts-->
                    <div class="row">
                        <div class="col-lg-8">
                            <% @reviews.each do |review|%>
                                <div class="card mb-4">
                                <div class="card-body">
                                    <div class="small text-muted"><%=review.created_at%></div>
                                    <h2 class="card-title h4"><%= review.author_name%></h2>
                                    <p class="card-text"><%=review.body%></p>
                                </div>
                            </div>
                            <%end%>
                        </div>
                    </div>